//promise

// function(resolve, reject) {
//     resolve('success');
//     reject('error');
// }

// async await

// async function always returns a promise, other values are wrapped in promise automatically, await waits for promise to be resolved
